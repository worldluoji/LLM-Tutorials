# Milvus Demo

可能的错误：
```
hint: You're on macOS (`macosx_14_0_x86_64`), but `torch` (v2.7.0) only has wheels for the following platforms: `manylinux_2_28_aarch64`, `manylinux_2_28_x86_64`, `macosx_11_0_arm64`, `win_amd64`
```
torch2.7已经不支持x86版本的macos