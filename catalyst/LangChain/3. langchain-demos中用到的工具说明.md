#  langchain-demos中用到的工具说明

## VectorDBQA
VectorDBQA是一个基于VectorDB和LLM的QA（问答）系统。
它使用VectorDB作为其存储和查询引擎，并利用LLM（Large Language Model）来理解问题和生成答案。

VectorDBQA的核心思想是将问题和答案都转化为向量表示，然后使用VectorDB的相似度搜索算法在大量的向量数据中找到最相似的答案。
这种方式可以有效地处理自然语言问题，并从大量的文本数据中找到相关的答案。

VectorDBQA的实现包括以下几个主要步骤：
- 问题向量化：将问题转化为向量表示。这可以通过使用LLM或其他自然语言处理技术来实现。
- 搜索相似答案：使用VectorDB的相似度搜索算法在大量的向量数据中找到最相似的答案。
- 答案生成：从找到的相似答案中提取有用的信息，并生成最终的答案。这可能需要使用一些自然语言处理技术，例如文本摘要或抽取式生成等。

VectorDBQA可以应用于各种自然语言问答任务，例如用户查询、聊天机器人、智能助手等。
通过使用VectorDBQA，可以提高问答系统的准确性和效率，为用户提供更好的服务体验。

---

## FAISS
FAISS（Facebook AI Similarity Search）是一个由Facebook开发的库，用于高效地执行向量最邻近检索。
它是专门为稠密向量设计的，可以快速地找到最相似的向量。FAISS的核心是使用GPU加速的K-最近邻搜索算法，可以在短时间内处理大量的数据。

FAISS的主要特点是它的高效性和可扩展性。它使用分布式计算技术，可以轻松地处理大规模数据集。
此外，FAISS还支持多种不同的数据存储方式，包括In-memory、On-disk和Parameter Server等。
这使得FAISS具有灵活性和可扩展性，可以满足不同用户的需求。

在NLP领域，FAISS已经被广泛应用于各种任务中，例如文本分类、实体识别、情感分析等。
通过使用FAISS，可以将文本表示为向量，并快速地找到最相似的文本。这可以帮助我们更好地理解文本数据，并对其进行分类和聚类。

总之，FAISS是一个非常实用的向量检索工具，它可以为稠密向量提供高效相似度搜索。
在NLP领域中，FAISS已经被广泛应用于各种任务中，提高了NLP任务的准确性和效率。

---

## SpacyTextSplitter 替代方案
由于spacy对python3.13支持不佳，可以将 `SpacyTextSplitter` 替换为 `RecursiveCharacterTextSplitter`，这是 LangChain 中一个不依赖 Spacy 的通用文本分割器。以下是修改后的代码方案：

```python
from langchain.text_splitter import RecursiveCharacterTextSplitter  # 替换这里

# 修改原 SpacyTextSplitter 的代码部分
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=256,
    chunk_overlap=20,
    separators=["\n\n", "\n", "。", "！", "？", "，", "、", ""]  # 中文适用的分隔符
)
```

完整修改后的代码示例：

```python
# 原导入部分修改为
from langchain.text_splitter import CharacterTextSplitter, RecursiveCharacterTextSplitter  # 移除 SpacyTextSplitter

# 原 SpacyTextSplitter 部分修改为
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=256,
    chunk_overlap=20,
    separators=["\n\n", "\n", "。", "！", "？", "，", "、", ""]
)
```

关键修改点说明：

1. **替换导入**：移除 `SpacyTextSplitter`，改用 `RecursiveCharacterTextSplitter`

2. **参数调整**：
   - `chunk_size=256` 保持与原配置一致
   - 新增 `chunk_overlap=20` 保持上下文连贯性
   - `separators` 参数特别添加了中文标点符号作为分隔符

3. **中文适配**：通过设置中文标点符号（句号、感叹号、问号、逗号）作为分隔符，替代 Spacy 的中文分句功能

4. **保持 CSV 处理方式**：保留原有的 `CharacterTextSplitter` 处理 CSV 数据（因其已使用换行符分隔）

优势：
- 完全移除对 Spacy 的依赖
- 更轻量的文本分割实现
- 显示指定中文适用的分隔符
- 保持原有 chunk_size 的上下文连续性

其他可选项：
如果效果不理想，可以尝试 `ChineseTextSplitter`（需安装 `textsplitter` 包）：
```bash
pip install textsplitter
```
```python
from textsplitter import ChineseTextSplitter
text_splitter = ChineseTextSplitter(chunk_size=256)
```

但 `RecursiveCharacterTextSplitter` 方案无需额外安装依赖，更适合你的使用场景。建议先尝试这个方案，如果分割效果不理想再考虑其他选项。

---

## OpenAIEmbeddings 替代方案
截止至2025-4-10， 由于DeepSeek没有 OpenAI Embeddings 相关的API, 如果还需要使用调用大模型API的方式做Embedding，可以尝试
使用其它提供了Embedding API的大模型，比如阿里的[text-embedding-v3](https://bailian.console.aliyun.com/?tab=doc#/list/?type=model&url=https%3A%2F%2Fhelp.aliyun.com%2Fdocument_detail%2F2840914.html)。

另一种方案就是本地化embedding。

---

## 本地化embbedding
针对完全本地化的 embedding 需求，这里提供 3 种无需 API 调用的替代方案及其实现方法：

---

### **方案一：使用 Sentence Transformers（推荐）**
使用 Hugging Face 的本地预训练模型，适合大多数中文场景：

```python
# 先安装依赖
# pip install sentence-transformers

from langchain_community.embeddings import HuggingFaceEmbeddings

# 使用多语言模型（支持中文）
model_name = "sentence-transformers/paraphrase-multilingual-mpnet-base-v2"
embeddings = HuggingFaceEmbeddings(
    model_name=model_name,
    model_kwargs={'device': 'cpu'},  # 指定使用 CPU
    encode_kwargs={'normalize_embeddings': False}
)

# 使用方式与原代码完全一致
docsearch = FAISS.from_documents(texts, embeddings)
```

---

### **方案二：轻量级本地词嵌入**
使用本地预训练词向量 + 平均池化（适用于简单场景）：

```python
import numpy as np
from gensim.models import KeyedVectors
from langchain_core.embeddings import Embeddings

# 1. 下载预训练词向量（中文）
w2v_model = KeyedVectors.load_word2vec_format('zh.vec')

class LocalWordEmbeddings(Embeddings):
    def embed_documents(self, texts: list) -> list:
        return [self._embed_text(t) for t in texts]

    def embed_query(self, text: str) -> list:
        return self._embed_text(text)

    def _embed_text(self, text: str) -> list:
        vectors = []
        for word in jieba.cut(text):  # 需要中文分词
            if word in w2v_model:
                vectors.append(w2v_model[word])
        if vectors:
            return np.mean(vectors, axis=0).tolist()
        return np.zeros(w2v_model.vector_size).tolist()

# 初始化
embeddings = LocalWordEmbeddings()
```

---

### **方案三：微型本地嵌入模型**
使用微型 ONNX 格式模型（资源占用最小）：

```python
from onnxruntime import InferenceSession
import numpy as np

# 下载微型模型（约 50MB）
# https://github.com/netease-youdao/BCEmbedding
session = InferenceSession("bce-embedding_quant.onnx")

class BCEEmbeddings(Embeddings):
    def __init__(self):
        self.input_name = session.get_inputs()[0].name
        
    def embed(self, text: str) -> list:
        # 需要自行添加文本预处理
        inputs = {"input_ids": ...} 
        return session.run(None, inputs)[0].tolist()[0]

    def embed_documents(self, texts: list) -> list:
        return [self.embed(t) for t in texts]

    def embed_query(self, text: str) -> list:
        return self.embed(text)

# 初始化
embeddings = BCEEmbeddings()
```

---

### **关键对比**

| 方案                | 优点                          | 缺点                          | 适用场景                   |
|---------------------|-------------------------------|-------------------------------|---------------------------|
| Sentence Transformers | 质量高、支持多语言            | 内存占用较大（约 1GB）        | 生产环境                  |
| Word2Vec平均        | 资源占用小                    | 丢失上下文语义                | 简单分类/聚类             |
| ONNX微型模型        | 超低资源（<100MB）            | 需自行处理预处理              | 移动端/边缘设备           |

---

### **最佳实践建议**
1. **中文优化**：在 `HuggingFaceEmbeddings` 中推荐使用以下模型：
   ```python
   # 专为中文优化的模型
   model_name = "GanymedeNil/text2vec-large-chinese"
   ```

2. **性能调优**：
   ```python
   # 启用 GPU 加速（如果有）
   HuggingFaceEmbeddings(model_kwargs={'device': 'cuda:0'})
   ```

3. **维度兼容**：
   ```python
   # 查看 embedding 维度
   print(len(embeddings.embed_query("test"))) 
   # 确认与原有代码的维度一致（如原 OpenAI 是 1536 维）
   ```

4. **内存优化**：
   ```python
   # 量化模型（trade-off 精度）
   HuggingFaceEmbeddings(encode_kwargs={'normalize_embeddings': True})
   ```

---

## PyTorch报错
```
luke-surface-mac@Luke-Surface-macdeMacBook-Pro langchain-demos % sudo uv add sentence-transformers
Password:
Resolved 90 packages in 25.86s
error: Distribution `torch==2.6.0 @ registry+https://pypi.org/simple` can't be installed because it doesn't have a source distribution or wheel for the current platform

hint: You're on macOS (`macosx_14_0_x86_64`), but `torch` (v2.6.0) only has wheels for the following platforms: `manylinux_2_28_aarch64`, `manylinux1_x86_64`, `macosx_11_0_arm64`, `win_amd64`
```
PyTorch 2.0.1 是最后一个官方支持 macOS x86_64 的版本。

解决该问题最直接的方案久是换一台设备。否则需要先把python版本降级，因为python3.13已经不支持pytorch2.0.1
```
× No solution found when resolving dependencies:
  ╰─▶ Because torch==2.0.1 has no wheels with a matching Python ABI tag (e.g., `cp313`) and you require torch==2.0.1,
      we can conclude that your requirements are unsatisfiable.

      hint: You require CPython 3.13 (`cp313`), but we only found wheels for `torch` (v2.0.1) with the following Python
      ABI tags: `cp38`, `cp39`, `cp310`, `cp311`
```
然后再安装：
```bash
# 先安装指定版本 PyTorch（支持 macOS x86_64）
pip install torch==2.0.1 torchvision==0.15.2 torchaudio==2.0.2 --index-url https://download.pytorch.org/whl/cpu

# 安装兼容版本的 transformers
pip install transformers==4.30.2

# 安装兼容的 sentence-transformers（建议 2.2.2 版本）
pip install sentence-transformers==2.2.2 --no-deps  # 跳过自动安装依赖
```