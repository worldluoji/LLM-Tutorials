[project]
name = "mcp-streamable-demo-client"
version = "0.1.0"
description = "Streamable HTTP MCP client demo"
readme = "../README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.10.0",
    "httpx>=0.27.0",
]

[tool.uv.sources]
mcp = { index = "pypi" }
